pytest --full-trace - for debugging
pytest -v -s
pytest -m smoke -v
-m is used to filter test execution based on pytest markers such as smoke, regression, or sanity.
-v enables verbose output to clearly display each test case name and its execution result.
To run test in parallel
pip install pytest-xdist
pytest -n 4 - Runs tests on 4 workers (threads/processes)


pytest -v --alluredir=allure-results ; allure serve allure-results

# Chromium (default)
pytest -m smoke

# Firefox
pytest -m smoke --browser-name=firefox

# Edge
pytest -m smoke --browser-name=msedge

# All 3 browsers
pytest -m smoke --browser-name=chromium ; pytest -m smoke --browser-name=firefox ; pytest -m smoke --browser-name=msedge


allure
pytest -m smoke --browser-name=chromium --alluredir=allure-results/chromium
pytest -m smoke --browser-name=firefox --alluredir=allure-results/firefox
pytest -m smoke --browser-name=msedge --alluredir=allure-results/msedge